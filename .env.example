GOSSIPMEMO_DATABASE_PATH=/data/gossipmemo.db
GOSSIPMEMO_HOST=127.0.0.1
GOSSIPMEMO_PORT=8765
# Canonical name used for the fixed current user in prompts and projections.
GOSSIPMEMO_USER_NAME=CurrentUser

# Optional bearer token required from SDK clients.
GOSSIPMEMO_API_KEY=

# Required OpenAI-compatible chat-completions configuration.
# GossipMemo has no provider URL, model, or API-key fallback.
GOSSIPMEMO_LLM_BASE_URL=
GOSSIPMEMO_LLM_API_KEY=
GOSSIPMEMO_LLM_MODEL=
GOSSIPMEMO_LLM_TIMEOUT_SECONDS=120
GOSSIPMEMO_LLM_MAX_RETRIES=5
GOSSIPMEMO_LLM_RETRY_BASE_SECONDS=1
GOSSIPMEMO_LLM_RETRY_MAX_SECONDS=30
GOSSIPMEMO_LLM_MAX_TOKENS=
GOSSIPMEMO_LLM_CONTEXT_WINDOW_TOKENS=65536
GOSSIPMEMO_LLM_OUTPUT_RESERVE_TOKENS=8192
GOSSIPMEMO_LLM_CONTEXT_SAFETY_TOKENS=4096
GOSSIPMEMO_EXTRACTION_BATCH_SIZE=6
GOSSIPMEMO_EXTRACTION_BATCH_TIMEOUT_SECONDS=1800

# Optional TOML file overriding individual prompt fragments by name.
# See prompts.example.toml; an unknown key fails at startup.
GOSSIPMEMO_PROMPTS_PATH=

# Local 24-hour HH:MM time of the daily profile-induction run.
GOSSIPMEMO_INDUCTION_TIME=00:00

# Structured application logs (production defaults to JSON at INFO).
GOSSIPMEMO_LOG_LEVEL=INFO
GOSSIPMEMO_LOG_FORMAT=json

# Optional OpenAI-compatible embeddings configuration. Leaving
# GOSSIPMEMO_EMBEDDING_MODEL empty turns the whole embedding subsystem off --
# GossipMemo falls back to plain FTS recall, not a startup failure.
# GOSSIPMEMO_EMBEDDING_BASE_URL and _API_KEY default to the GOSSIPMEMO_LLM_*
# values above when left empty. GOSSIPMEMO_EMBEDDING_BASE_URL is an API root
# that already includes /v1, matching GOSSIPMEMO_LLM_BASE_URL's convention
# (e.g. http://192.168.1.113:8002/v1).
GOSSIPMEMO_EMBEDDING_BASE_URL=
GOSSIPMEMO_EMBEDDING_API_KEY=
GOSSIPMEMO_EMBEDDING_MODEL=
# Fallback dimension used only if the server does not expose /v1/models'
# meta.n_embd for the configured model. If both are present and disagree,
# startup logs the conflict and disables embedding rather than guessing.
GOSSIPMEMO_EMBEDDING_DIM=
